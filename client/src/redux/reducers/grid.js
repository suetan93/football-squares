import Redux from 'redux';

//create a reducer for the grid

const gridReducer = (state = new Array(9), action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default gridReducer
